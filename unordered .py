# def m(unordered_lists):
#     i=0
#     while i<len(unordered_lists)-1:
#         j=0
#         while j<len(unordered_lists)-1:
#             if unordered_lists[j]>unordered_lists[j+1]:
#                 unordered_lists[j],unordered_lists[j+1]=unordered_lists[j+1],unordered_lists[j]
#             j+=1
#         i+=1
#     print(unordered_lists)
# unordered_lists = [6, 8, 4, 3, 9, 56, 0, 34, 7, 15]
# m(unordered_lists)
        